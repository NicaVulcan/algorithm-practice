// Write code to print all even numbers from 0 to `num`
// Assume `num` will be a positive number

var logEvenNums = function(num) {
    //loop through all even numbers from 0 to num
    for (let i = 0; i <= num; i += 2) {
        //log each number
        console.log(i);
    }
};
